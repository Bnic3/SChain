package com.nana.app

import java.io.File

import com.nana.common.{BlockLogging, MyDB}
import io.iohk.iodb.LSMStore
import com.nana.common.MyConstants._

class core extends App with BlockLogging with MyDB{

//  val iFile = new File("/scalachain/block")
//  iFile.mkdir()
//  val blockStorage = new LSMStore(iFile, maxJournalEntryCount = 10000)
//  val test = BLOCKNAMESPACE

}
